#!/bin/bash

#
# Copyright (C) 2026 Nethesis S.r.l.
# SPDX-License-Identifier: GPL-3.0-or-later
#
# create-module never runs again, so a secret added to the module later would
# never exist on an older instance. Before 20restart, so the file is complete
# when the services come back.
#

set -e
exec 1>&2 # Redirect any output to the journal

exec generate-secrets
