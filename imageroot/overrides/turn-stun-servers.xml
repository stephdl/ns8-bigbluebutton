<?xml version="1.0" encoding="UTF-8"?>
<!--
    Replaces /etc/bigbluebutton/turn-stun-servers.xml.tmpl from
    alangecker/bbb-docker-web. Still a dockerize template, rendered by bbb-web's
    entrypoint, so {{ .Env.X }} is resolved there.

    Two changes, both because this module ships no coturn: upstream's unguarded
    turn0 bean is gone, and stun1 is guarded so an empty value cannot produce an
    invalid bean.
-->
<beans xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-2.5.xsd">

    {{if .Env.STUN_SERVER }}
    <bean id="stun1" class="org.bigbluebutton.web.services.turn.StunServer">
        <constructor-arg index="0" value="{{ .Env.STUN_SERVER }}"/>
    </bean>
    {{end}}

    {{if .Env.TURN_EXT_SERVER }}
        <bean id="turn2" class="org.bigbluebutton.web.services.turn.TurnServer">
            <constructor-arg index="0" value="{{ .Env.TURN_EXT_SECRET }}"/>
            <constructor-arg index="1" value="{{ .Env.TURN_EXT_SERVER }}"/>
            <constructor-arg index="2" value="86400"/>
        </bean>
    {{end}}

    <bean id="stunTurnService" class="org.bigbluebutton.web.services.turn.StunTurnService">
        <property name="stunServers">
            <set>
                {{if .Env.STUN_SERVER }}
                <ref bean="stun1" />
                {{end}}
            </set>
        </property>
        <property name="turnServers">
            <set>
                {{if .Env.TURN_EXT_SERVER }}
                <ref bean="turn2" />
                {{end}}
            </set>
        </property>
        <property name="remoteIceCandidates">
            <set>
            </set>
        </property>
    </bean>
</beans>
